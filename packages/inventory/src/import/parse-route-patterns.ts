/**
 * 头部解析与正文解析共用的几个模式。
 *
 * 单独放一处,是为了让两边认的写法始终是同一套——天数在头部要认、在校验
 * 天数是否吻合时也要认,各写一份迟早会漂开。
 */

/** 天数与晚数,如「12 天 11 晚」;中间可能夹空格或全角空格。 */
export const DAYS_NIGHTS = /(\d+)\s*天\s*(\d+)\s*晚/

/** 井号标签,如 #私家出行#1 动#乌起喀止。 */
export const TAG = /#([^#\s　]+)/g
